{ config, pkgs, inputs, ... }:

{
  # sops.defaultSopsFile = ./secrets/secrets.yaml;
  # sops.defaultSopsFormat = "yaml";
  #
  # sops.age.keyFile = "/home/ester/.config/sops/age/keys.txt";
  #
  # sops.secrets."networks/network1/name" = { };
  # sops.secrets."networks/network1/password" = { };
}
